/**
 * GXPM - Gold/XP per Minute mark
 * An array of minute marks with corresponding values that can be displayed in an am4Chart component.
 */
export interface GXPM {
    minute: number;
    value: number;
}
